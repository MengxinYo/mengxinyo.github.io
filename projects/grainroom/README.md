# Grainroom

Self-contained browser image editor embedded by `vibe-coding.html`.

- `index.html` — semantic interface and controls
- `grainroom.css` — responsive visual system
- `grainroom.js` — local image processing, preview, and PNG export

The project has no dependencies or build step. Images stay in the browser and are never uploaded.
